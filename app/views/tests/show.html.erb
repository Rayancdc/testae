<div class="container">
  <div class="row">
    <div class="col-xs-12">
  <div>
    <h3>Descrição do Teste:</h3>
  </div>
    <div class="card">
      <div class="card-body"> <p><%= @test.description %></p> </div>
  </div>

  <div>
  <h3>Accesse o link abaixo para testar:</h3>
  </div>
    <div class="form-control" <p> <%= @test.website %> </p></div>
  </div>
    </div>
    <br>
    <h2>Ganhe R$<%= @test.issue_price_cents/100 %> por bug</h2>
      <div class="list-inline">
        <%= link_to "Ir para o website", new_test_review_path(@test), class: "btn btn-danger btn-lg" %>
        <%= link_to "Incluir Review", new_test_review_path(@test), class: "btn btn-danger btn-lg" %>
      </div>
      <br>

      <h2>Ganhe R$<%= @test.review_price_cents/100 %> por review</h2>
        <div class="list-inline">
        <%= link_to "Ir para o website", new_test_review_path(@test), class: "btn btn-danger btn-lg" %>
        <%= link_to "Incluir Review", new_test_review_path(@test), class: "btn btn-danger btn-lg" %>
      </div>
    </div>
  </div>
  </div>

